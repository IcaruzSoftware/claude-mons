{
  "name": "@claude-mons/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Dependency-free game logic shared by the desktop app and the Supabase Edge Functions (Node + Deno).",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*"
  },
  "scripts": {
    "build": "echo \"shared is consumed as TypeScript source; nothing to build\"",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "sim": "node --experimental-strip-types scripts/sim.ts"
  }
}
